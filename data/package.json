{
  "name": "ust-rankings-data",
  "type": "module",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "update:cq": "cd data/cq && git fetch --depth=1 && git reset --hard origin",
    "update:review": "cd data/review && git fetch --depth=1 && git reset --hard origin",
    "update:sfq": "cd data/sfq && git fetch --depth=1 && git reset --hard origin",
    "update": "bun run update:cq && bun run update:review && bun run update:sfq",
    "lint": "eslint --fix",
    "type-check": "tsc",
    "run:inspect-data": "bun src/data",
    "run:inspect-stats": "bun src/stats",
    "run": "bun src/stats"
  },
  "license": "MIT",
  "dependencies": {
    "@sinclair/typebox": "^0.34.37",
    "es-main": "^1.3.0",
    "es-toolkit": "^1.39.5",
    "glob": "^11.0.3",
    "mathjs": "^14.5.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "@stylistic/eslint-plugin": "^5.1.0",
    "@types/bun": "^1.2.17",
    "eslint": "^9.30.0",
    "globals": "^16.2.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0"
  }
}
